@model IEnumerable<EntityFrameworkDatabaseFirst.MUser>

@{
    ViewBag.Title = "Index";
}

<h2>WEB GRID</h2>

@{
    var grid = new WebGrid(
        Model, rowsPerPage: 5,
        defaultSort: "Name",
        columnNames: new[] { "FirstName", "LastName", "MiddleName", "Password", "Address", "Email" }
        );
 } 

 @grid.GetHtml(
    tableStyle: "table button bc",
    fillEmptyRows: false,
    mode: WebGridPagerModes.All,
    columns: grid.Columns(
        grid.Column("FirstName", header: "FirstName " + EntityFrameworkDatabaseFirst.Models.helpers.SortDirection(null, ref grid, "FirstName")),
        grid.Column("LastName", header: "LastName " + EntityFrameworkDatabaseFirst.Models.helpers.SortDirection(null, ref grid, "LastName")),
        grid.Column("MiddleName", header: "MiddleName " + EntityFrameworkDatabaseFirst.Models.helpers.SortDirection(null, ref grid, "MiddleName")),
        grid.Column("Password", header: "Password"),
        grid.Column("Address", header: "Address " + EntityFrameworkDatabaseFirst.Models.helpers.SortDirection(null, ref grid, "Address")),
        grid.Column("Email", header: "Email")
))

<p>
    @*@Html.ActionLink("Create New", "Create")*@
</p>
<table class="table button">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LastName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MiddleName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Password)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Address)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Email)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LastName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MiddleName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Password)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Address)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Email)
        </td>
        <td>

    @Html.ActionLink("Edit", "Edit", new { id = item.MUserID }) <span>|</span>
    @Html.ActionLink("Details", "Details", new { id = item.MUserID }) <span>|</span>
            @Html.ActionLink("Delete", "Delete", new { id = item.MUserID })

        </td>
    </tr>
}

</table>
